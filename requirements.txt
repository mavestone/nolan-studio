# Nolan Studio — Python dependencies
# Tested with Python 3.11

# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.2
# Starlette 1.0.0+ broke compatibility with this FastAPI; pin to last 0.46.x
starlette>=0.46.0,<0.47.0
python-multipart==0.0.22

# Database
aiosqlite==0.22.1

# Transcription (local Whisper)
faster-whisper==1.2.1
av==17.0.1

# Scene detection
# Don't use the [opencv] extra — we install opencv-python-headless directly.
scenedetect>=0.6.5
opencv-python-headless>=4.10,<5.0

# AI providers
anthropic==0.97.0
groq==1.2.0
google-genai==1.74.0

# Telegram bot
python-telegram-bot[job-queue]==22.7

# Env config
python-dotenv==1.1.0
